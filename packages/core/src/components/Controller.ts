export default class Controller {
	constructor(
		/**
		 * The speed of the entity in meters per second
		 */
		public speed = 200,
		/**
		 * Jump force in newtons
		 */
		public jumpForce = 3500
	) {}
}
